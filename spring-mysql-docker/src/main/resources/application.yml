# The commented one is the credentials for local database
# url: jdbc:mysql://localhost:3306/db
# username: root
# password: qwerty12345

spring:
  datasource:
    url: jdbc:mysql://mysql-standalone:3306/db
    username: sa
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
#    properties:
#      hibernate:
#       dialect: org.hibernate.dialect.MySQL5Dialect

server:
  port: 8081